[project]
name = "syncfs"
version = "1.0.0"
description = "Syncfs file synchronizing"
authors = [
    {name = "Pablo Carbonell Garcia", email = "your.email@example.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.0",
    "pytest-repeat (>=0.9.4,<0.10.0)",
]
package-mode = false

[tool.pytest.ini_options]
testpaths = ["tests"]
